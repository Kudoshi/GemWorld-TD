using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DebugMode : MonoBehaviour
{
    //Set debug mode to true to enable debugging mode
    public static bool debugMode = true;
    public static bool debugUnitKilled = true;  //Outputs unit killed if true
    
}
